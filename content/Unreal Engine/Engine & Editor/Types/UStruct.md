
`ChildProperties` is the first `FField` of the linked list. (See [[FField]] for more info)

# TCppStructOps

- To see what options are available see `ICppStructOps::FCapabilities`.
- To see what functions can be implemented see `UScriptStruct::TCppStructOps`.
- See `TStructOpsTypeTraitsBase2` for actual name to use and descriptions.
