
Example: if you are toggling a flag state with a shortcut, `FShowFlagMenuCommands::ToggleShowFlag` is called.
This will call `FEngineShowFlags::SetSingleFlag`.

There is also `FEditorViewportClient::SetShowCollision`