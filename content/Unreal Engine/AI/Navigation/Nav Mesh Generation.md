
The virtual function `UActorComponent::IsNavigationRelevant` which is mostly known for its implementation in `UPrimitiveComponent` will by default return true if the primitive has the channels Pawn and Vehicule as blocked.

