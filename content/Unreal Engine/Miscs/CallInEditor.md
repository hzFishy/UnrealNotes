
Apart from exposing the function in the details panel, this function meta attribute can also be added on function that you want to run on editor side. (for eg: a BNE function). Because by default it seems that BNE (and probably BIE) are not called in BPs if not playing.
